import { StyleSheet } from 'react-native';
import { globalColor, globalFonts, globalFontsSize } from "../../Constant";

const addCardPageStyles = StyleSheet.create({
    addCardContainer:{
        flex: 1,
        backgroundColor: 'rgba(0,0,0, 0.60)'
    },
    addCardIMage1: {
        height: '100%',
        width: '100%',  
    },
})


export default addCardPageStyles