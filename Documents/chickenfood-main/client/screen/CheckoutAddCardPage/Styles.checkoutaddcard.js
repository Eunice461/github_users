import { StyleSheet } from 'react-native';
import { globalColor, globalFonts, globalFontsSize } from "../../Constant";

const CheckoutAddCardPageStyles = StyleSheet.create({
    CheckoutAddCardContainer:{
        flex: 1,
        backgroundColor: 'rgba(0,0,0, 0.60)'
    },
    CheckoutAddCardIMage1: {
        height: '100%',
        width: '100%',  
    },
})


export default CheckoutAddCardPageStyles